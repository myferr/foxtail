######################################################################################
#
# This workflow is a build & release workflow for Crystal
# 
# https://github.com/catto-oss/cookbook
# 
######################################################################################

name: Crystal Build & Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  build-linux:
    name: Build Linux x86_64 (Crystal)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libssl-dev libpcre3-dev libyaml-dev libgmp-dev \
            build-essential pkg-config

      - name: Install Crystal
        uses: crystal-lang/install-crystal@v1
        with:
          crystal: latest

      - name: Install shards
        run: shards install

      - name: Create bin directory
        run: mkdir -p bin

      - name: Build
        run: |
          crystal build --release -o bin/binary-linux-x86_64 cli.cr

      - uses: actions/upload-artifact@v4
        with:
          name: binary-linux-x86_64
          path: bin/binary-linux-x86_64

  build-macos-x86_64:
    name: Build macOS x86_64 (Crystal)
    runs-on: macos-15-intel   # Intel runner
    steps:
      - uses: actions/checkout@v4

      - name: Install deps
        run: brew install openssl@3 pcre libyaml gmp

      - name: Install Crystal
        uses: crystal-lang/install-crystal@v1
        with:
          crystal: latest

      - run: shards install

      - name: Create bin directory
        run: mkdir -p bin

      - name: Build
        run: |
          crystal build --release -o bin/binary-macos-x86_64 cli.cr

      - uses: actions/upload-artifact@v4
        with:
          name: binary-macos-x86_64
          path: bin/binary-macos-x86_64

  build-macos-aarch64:
    name: Build macOS arm64 (Crystal)
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install deps
        run: brew install openssl@3 pcre libyaml gmp

      - name: Install Crystal
        uses: crystal-lang/install-crystal@v1
        with:
          crystal: latest

      - run: shards install

      - name: Create bin directory
        run: mkdir -p bin

      - name: Build
        run: |
          crystal build --release -o bin/binary-macos-aarch64 cli.cr

      - uses: actions/upload-artifact@v4
        with:
          name: binary-macos-aarch64
          path: bin/binary-macos-aarch64

  release:
    name: Publish GitHub Release
    needs:
      - build-linux
      - build-macos-x86_64
      - build-macos-aarch64
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/download-artifact@v4
        with:
          path: release

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            release/**/binary-linux-x86_64
            release/**/binary-macos-x86_64
            release/**/binary-macos-aarch64
